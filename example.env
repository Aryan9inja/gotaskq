# Server
PORT=8000

# Retry settings (per-job max_retries still controls retries)
MAX_RETRIES=5
BASE_DELAY=100
MAX_DELAY=5000

# Worker pool
NUM_WORKERS=10

# Redis backend
USE_REDIS=false
REDIS_URL=redis://localhost:6379

# Job key expiration (in minutes)
COMPLETE_JOB_TTL=5